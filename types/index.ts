export interface Skill {
  name: string;
  id: string;
}

export interface Education {
  id: string;
  degreeName: string;
  institutionName: string;
}

export interface Option {
  value: string;
  label: string;
}